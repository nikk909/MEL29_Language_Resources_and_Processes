# MEL29 Language Resources and Processes

Char vs Word language modeling: RNN vs Transformer (PyTorch).

## Setup (in progress)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

Main notebook: `CharVSWordGPT_YueMa.ipynb` (section 1 bootstrap).

More documentation will be added in later commits.
